# AI 一角焕新 · 工程交接

本文是项目的持续工程事实来源。代码、配置、架构、行为、数据边界、构建或发布流程发生变化时，必须同步更新对应章节，不能只追加聊天式变更记录。

## 1. 当前状态与成熟度

当前版本 `0.1.0` 是“运行时静态、交互可演示”的比赛 MVP。

已经实现：

- 从三组演示灵感中选择目标风格。
- 展示灵感的风格、颜色、材质、可迁移元素和不照搬元素。
- 使用默认书桌样例，或在浏览器本地预览用户上传图片。
- 设置预算、不打孔、保留桌椅、宠物和本次改善目标。
- 展示模拟的理解、检索、规划、生成和校验过程。
- 查看三套结构化方案、前后效果、商品清单、摆放步骤、教程和校验结果。
- 将方案压缩到 300 元目标附近。
- 使用 `localStorage` 保存方案标识。
- 适配桌面与移动端。

尚未实现：

- 真实抖音内容、商品、账号或交易接口。
- 真实多模态空间识别和连续视频理解。
- 真实商品召回、库存检查和在线价格更新。
- 三套方案各自独立、商品一致的效果图。
- 服务端账号、数据持久化、多人协作和生产监控。
- 厘米级测量、硬装设计或施工建议。

不得把当前版本描述为生产级空间设计服务。效果图是视觉示意，布局、尺寸与商品仍需复测。

## 2. 用户行为与状态

主路径：

```text
empty
→ inspiration_selected
→ room_ready
→ constraints_ready
→ analyzing
→ result_ready
→ adjusting
→ saved | discarded
```

当前状态实现：

- 初始：原木灵感、默认书桌样例、预算 500 元。
- 灵感：切换灵感会更新提取结果，并作为默认方案选择。
- 房间：上传图片通过 `FileReader` 创建本地 Data URL。
- 约束：预算滑块和选择项只存在于当前页面内存。
- 分析：定时器模拟 Workflow 进度。
- 结果：方案数据来自 `apps/web/data/demo-data.js`。
- 调整：降预算会切换到高效收纳方案，并移除一项样例商品。
- 保存：只在 `localStorage` 保存方案 key。
- 刷新：用户图片和临时约束清除；保存状态可以恢复。

必须在后续覆盖的降级状态：

- 图片不清晰，需要补拍或确认。
- 缺少参考尺寸。
- 预算低于可行商品组合。
- 没有符合安装限制的商品。
- 效果图结构保持校验失败。
- 内容或商品接口不可用。

## 3. 核心对象与所有权

稳定对象：

- `RoomProfile`：真实空间的稳定身份。拥有固定结构、尺寸假设、已有家具、环境和风险信息。
- `InspirationProfile`：一次内容理解结果。可独立替换，不直接修改空间。
- `UserConstraints`：本次任务的硬约束、软偏好、预算和保留项。
- `DesignPlan`：可版本化的方案快照。拥有商品、位置、理由、步骤、预算和效果图引用。
- `ValidationReport`：针对一个方案版本的尺寸、预算、结构、库存和安全结果。
- `AICard`：面向前端的组合视图，不是底层事实来源。

可替换对象：

- 用户原图和预生成效果图。
- 商品、教程和达人案例候选。
- 模型识别结果及其置信度。
- 某次规划和检索执行结果。

规则归属：

- 硬约束定义属于协议层。
- 商品过滤和替代属于检索/校验层。
- 界面只呈现规则结果，不自行发明业务判断。
- Prompt 不应成为预算、尺寸和安全规则的唯一存储位置。

详细架构见 [`docs/architecture.md`](docs/architecture.md)。

## 4. 代码与模块

### `apps/web`

- `index.html`：语义结构、四步工作区、结果卡片和弹窗。
- `styles.css`：纸张编辑部视觉系统、响应式布局和动效。
- `app.js`：浏览器状态、步骤跳转、图片预览、方案渲染、保存与调整。
- `data/demo-data.js`：灵感、方案、商品、步骤和 Agent Trace 样例。
- `assets/desk-before.png`：默认空间样例。
- `assets/desk-after-warm.png`：AI 焕新效果样例。

### 根目录

- `scripts/serve.mjs`：无依赖静态服务器，仅用于本地预览。
- `scripts/validate.mjs`：必要文件、本地引用和敏感内容检查。
- `.github/workflows/validate.yml`：推送和 PR 时运行 `npm run check`。

## 5. 配置

当前没有运行时环境变量、外部依赖或后端配置。

约定：

- Node.js 版本不低于 20。
- 本地服务器默认监听 `127.0.0.1:8765`。
- 新增真实服务时必须通过环境变量配置 URL 和密钥。
- `.env` 永不提交；只允许提交不含秘密的 `.env.example`。
- 不允许把访问令牌写入前端代码、样例数据、测试夹具或截图。

## 6. 数据与隐私边界

当前版本：

- 没有后端和网络上传。
- 用户图片只存在于当前浏览器页面。
- `localStorage` 只保存方案 key。
- 商品、库存、价格和内容均为演示数据。
- 不包含真实抖音用户数据和平台私有数据。

接入真实服务前必须完成：

- 明示图片用途、保存周期和删除入口。
- 日志脱敏和最小化收集。
- 敏感画面检测。
- 内容作者、来源和跳转信息保留。
- 商品价格更新时间、库存状态和数据来源记录。
- 测试数据与生产数据隔离。

## 7. 本地运行、测试与构建

安装 Node.js 20 后：

```bash
npm run check
npm start
```

验证内容：

- `app.js` 与演示数据脚本语法。
- 所有必要文件存在。
- HTML 引用的本地资源存在。
- 仓库中不存在常见令牌和私钥模式。

当前无构建步骤。`apps/web` 可由任意静态服务器部署。生产化迁移到 React/Vite 时，应保留当前数据对象边界，不能把演示数据直接散落进组件。

手动验收：

1. 三种灵感均可选择，提取结果同步变化。
2. 本地上传图片后只替换预览。
3. 预算、约束和目标标签可操作。
4. 分析进度结束后正常展示结果。
5. 三套方案的标题、商品、价格、步骤和校验同步变化。
6. 前后对比滑杆可拖动。
7. 降预算后显示 4 件商品、合计 240 元。
8. 保存后刷新，保存按钮恢复，但用户图片不恢复。
9. 过程弹窗可开关。
10. 390px 和 1440px 无横向溢出和控制台错误。

## 8. 构建与交付

当前交付是源码静态站点。

推荐发布顺序：

1. `npm run check`
2. 本地走完三分钟 Demo
3. 合并 Pull Request 到 `main`
4. 使用 GitHub Pages、静态托管或比赛指定环境发布 `apps/web`
5. 核对发布版本、Commit SHA 和浏览器缓存

当前仓库首次以 Private 形式协作。切换 Public 前必须完成 [`docs/public-release.md`](docs/public-release.md)。

## 9. 三人协作

长期所有权划分：

- A：产品与体验——用户路径、抖音生态表达、验收和演示。
- B：前端与工程——H5、状态管理、CI、部署和交互质量。
- C：AI 与数据——协议、视觉理解、检索、校验和评测集。

姓名与 GitHub 账号由团队在 [`docs/team-roles.md`](docs/team-roles.md) 中填写。跨所有权修改必须由对应负责人 Review。三人均对文档、隐私和 Demo 稳定性负责。

## 10. 已知限制与风险

- 当前桌面大屏体验强于抖音原生轻流程，需要增加用户模式。
- 方案 02 和 03 共用方案 01 效果图，仅用色调表示差异。
- 空间识别、内容召回、商品库存和校验均为模拟。
- 单图不能可靠恢复真实尺寸和遮挡区域。
- 降预算逻辑目前是界面级演示，尚未形成通用规划规则。
- 演示图片不能直接推导真实商品一致性。
- 开源公开后需避免让第三方误认为项目是抖音官方产品。

## 11. 下一阶段

优先顺序：

1. 建立抖音原生用户模式，保留当前评委模式。
2. 为核心对象增加 JSON Schema 与固定评测样例。
3. 接入一个真实多模态接口，只替换空间理解模块。
4. 建立至少 30 条带尺寸、安装方式和库存状态的演示商品。
5. 实现预算、尺寸和安装限制的独立校验包。
6. 为三套方案生成独立效果图。
7. 增加异常降级和端到端自动化测试。
8. 完成公开发布审计，再将仓库切换为 Public。
